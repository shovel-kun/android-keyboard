# Cold release builds saturate a 2g daemon (peak live-after-GC 100% of heap during
# R8, ~86% during APK packaging, which then OOMs in zipflinger on CI); 4g leaves headroom.
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
android.enableJetifier=false
android.useAndroidX=true